<% if (msgs = record.errors).any? %>
  <section class='validation_errors'>
    <h1>
      <%= t('activerecord.errors.introduction', name: record.class.name) %>
    </h1>
    <ul>
      <% msgs.each do |_, msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </section>
<% end %>
