<%= render 'shared/errors', record: @gist %>

<%= form_for @gist do |f| %>
  <div class='row'>
    <%= f.label :remote_id, "Gist URL or ID" %>
    <%= f.text_field :remote_id %>
  </div>

  <div class='row'>
    <%= f.submit 'Save', class: 'button' %>
  </div>
<% end %>
